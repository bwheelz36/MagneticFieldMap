The only diference between these extensions and the original is that these ones print a info to the terminal

If you try and run this with the GUI, it will likely crash due to line 355 which attempts to print every queried field point to the screen. that seems to make the GUI crash.
